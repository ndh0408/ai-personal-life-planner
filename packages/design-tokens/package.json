{
  "name": "@lifeos/design-tokens",
  "version": "0.1.0",
  "private": true,
  "description": "Single source of truth for LifeOS design tokens (color, type, motion, space). TS today; Style Dictionary outputs to Swift/Kotlin in Phase 4.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist", "src"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "build:watch": "tsc -p tsconfig.json --watch",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "prepare": "tsc -p tsconfig.json"
  },
  "devDependencies": {
    "typescript": "^5.4.5"
  }
}
