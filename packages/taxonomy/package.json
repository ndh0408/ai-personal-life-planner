{
  "name": "@lifeos/taxonomy",
  "version": "0.1.0",
  "private": true,
  "description": "Canonical taxonomy: capture kinds, event taxonomy, privacy tiers, monetization tiers. Tiny on purpose — used by everyone.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist", "src"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "build:watch": "tsc -p tsconfig.json --watch",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "prepare": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "zod": "^3.23.4"
  },
  "devDependencies": {
    "typescript": "^5.4.5"
  }
}
