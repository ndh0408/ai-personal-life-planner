{
  "name": "@lifeos/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Shared types and Zod schemas between @lifeos/api and @lifeos/mobile",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "zod": "^3.23.4"
  },
  "devDependencies": {
    "typescript": "^5.4.5"
  }
}
