{
  "name": "@lifeos/aurora",
  "version": "0.1.0",
  "private": true,
  "description": "Aurora — Round 42 redesign. Dawn-to-dusk dynamic palette + glass surfaces + serif/sans extreme contrast. Replaces warm-dark v2 in the active app shell; v2 stays available for legacy screens.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist", "src"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "build:watch": "tsc -p tsconfig.json --watch",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "prepare": "tsc -p tsconfig.json"
  },
  "devDependencies": {
    "typescript": "^5.4.5"
  }
}
