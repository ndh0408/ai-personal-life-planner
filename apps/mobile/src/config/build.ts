/**
 * Build-time constants. Bumped manually with each release for now —
 * the CI pipeline (round 25) will inject the real Git SHA + build number.
 */
export const APP_VERSION = '0.32.0';
export const APP_BUILD = 'round-32';
