/**
 * Build-time constants. Bumped manually with each release for now —
 * the CI pipeline (round 9) will inject the real Git SHA / build number.
 */
export const APP_VERSION = '0.7.0';
export const APP_BUILD = 'round-7';
