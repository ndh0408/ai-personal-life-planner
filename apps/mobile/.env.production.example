# Production build target — AAB for Play Store via `eas build --profile production`.
# HTTPS is REQUIRED. app.config.ts will refuse to build on localhost or http://.
EXPO_PUBLIC_APP_ENV=production
EXPO_PUBLIC_API_BASE_URL=https://api.yourdomain.com/api
