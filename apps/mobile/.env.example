# =================================================================
# LifeOS AI — Mobile environment example
# Copy to .env. Used by Metro at bundle time (process.env.LIFEOS_*).
# =================================================================

# API base. Default is public HTTPS via Cloudflare Tunnel.
# Override only for staging or local-LAN dev (e.g. http://100.100.210.85:4000/api on Tailscale).
LIFEOS_API_BASE_URL=https://api.tothanhthuy.cloud/api
