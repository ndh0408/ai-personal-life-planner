# =================================================================
# LifeOS AI — Mobile environment example
# Copy to .env. Used by Metro at bundle time (process.env.LIFEOS_*).
# =================================================================

# API base — when on Tailscale, point to huy-server's tailnet IP.
LIFEOS_API_BASE_URL=http://100.100.210.85:4000/api
