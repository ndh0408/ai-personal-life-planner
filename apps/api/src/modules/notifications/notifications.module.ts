import { Module } from '@nestjs/common';

/**
 * Round 5 will add: device registration, local-first notifications,
 * BullMQ scheduling for daily nudges.
 */
@Module({})
export class NotificationsModule {}
