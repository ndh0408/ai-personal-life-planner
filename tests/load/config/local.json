{
  "_comment": "Local-dev profile. Used by tests/load/*.js when BASE_URL env is unset. NEVER edit to point at production — the helpers refuse on /prod|production|live/ in the URL.",
  "baseUrl": "http://127.0.0.1:3000",
  "users": 50,
  "password": "LoadTest!1",
  "seedPrefix": "loadtest+",
  "seedDomain": "lifeos.local",
  "expoPushDryRun": true,
  "aiProvider": "mock"
}
